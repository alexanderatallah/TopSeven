<div class="col-sm-4 saved-list">
{{#each article}}
  <div class="saved-list">
    <a href="#" class="list-group-item active">
      <div class="rank">{{#}}</div>
      <h4 class="list-group-item-heading">{{article title}}</h4>
      <p class="list-group-item-text">{{description}}</p>
    </a>
    </div>
    {{/each}}
</div>

